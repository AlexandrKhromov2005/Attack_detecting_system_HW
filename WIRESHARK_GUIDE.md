# Wireshark — Полное руководство по анализу сетевых атак

## Оглавление
1. [Введение и интерфейс](#введение-и-интерфейс)
2. [Начало работы с PCAP файлом](#начало-работы-с-pcap-файлом)
3. [Базовые фильтры отображения](#базовые-фильтры-отображения)
4. [Статистика и анализ](#статистика-и-анализ)
5. [Анализ HTTP трафика](#анализ-http-трафика)
6. [Анализ DNS трафика](#анализ-dns-трафика)
7. [Реконструкция TCP потоков](#реконструкция-tcp-потоков)
8. [Экспорт объектов](#экспорт-объектов)
9. [Продвинутые техники](#продвинутые-техники)
10. [Практические примеры анализа атак](#практические-примеры-анализа-атак)

---

## Введение и интерфейс

### Основные панели Wireshark

При открытии Wireshark вы увидите три основные панели:

```
┌──────────────────────────────────────────┐
│   1. Список пакетов (Packet List)       │
│   ────────────────────────────────────   │
├──────────────────────────────────────────┤
│   2. Детали пакета (Packet Details)     │
│   ────────────────────────────────────   │
├──────────────────────────────────────────┤
│   3. Байты пакета (Packet Bytes)        │
└──────────────────────────────────────────┘
```

**1. Список пакетов** — показывает все захваченные пакеты в хронологическом порядке
**2. Детали пакета** — показывает разобранные поля выбранного пакета (протоколы, заголовки)
**3. Байты пакета** — показывает сырые данные пакета в hex и ASCII формате

### Цветовая кодировка

Wireshark автоматически раскрашивает пакеты для быстрой идентификации:

| Цвет | Протокол/Тип |
|------|--------------|
| 🟩 Светло-зеленый | HTTP трафик |
| 🟦 Светло-голубой | DNS трафик |
| ⬛ Черный | TCP пакеты с ошибками |
| 🟥 Красный | Отклоненные пакеты |
| 🟨 Желтый | ICMP трафик |

Вы можете настроить цвета в: **View → Coloring Rules**

---

## Начало работы с PCAP файлом

### Шаг 1: Открытие файла

1. Запустите Wireshark
2. **File → Open** (или `Ctrl+O`)
3. Выберите ваш `.pcap` или `.pcapng` файл

### Шаг 2: Первичный осмотр

После открытия файла выполните следующие действия:

**1. Посмотрите общую информацию:**
   - **Statistics → Capture File Properties**
   - Здесь вы увидите:
     - Размер файла
     - Количество пакетов
     - Временной диапазон захвата
     - Средняя скорость передачи данных

**2. Определите активные хосты:**
   - **Statistics → Endpoints** → вкладка **IPv4**
   - Сортируйте по количеству пакетов (Packets) или байт (Bytes)
   - Самый активный хост обычно является зараженным

**3. Посмотрите TCP-соединения:**
   - **Statistics → Conversations** → вкладка **TCP**
   - Здесь вы увидите все соединения между хостами
   - Сортируйте по количеству пакетов для поиска аномалий

---

## Базовые фильтры отображения

### Применение фильтра

Фильтры вводятся в **верхней панели** под меню:
```
┌─────────────────────────────────────────┐
│ [  Фильтр здесь...               ] [▶] │
└─────────────────────────────────────────┘
```

После ввода фильтра нажмите **Enter** или кнопку **▶**

Если фильтр корректный — поле станет **зеленым** ✅
Если синтаксис неправильный — поле станет **красным** ❌

### Фильтры по IP-адресам

#### 1. Трафик от конкретного IP
```
ip.src == 192.168.1.100
```
**Применение:** Показать весь трафик, отправленный с зараженного хоста

---

#### 2. Трафик к конкретному IP
```
ip.dst == 10.0.0.1
```
**Применение:** Показать трафик, направленный к серверу C2

---

#### 3. Трафик с/на IP (любое направление)
```
ip.addr == 192.168.1.100
```
**Применение:** Полный трафик хоста (входящий и исходящий)

---

#### 4. Несколько IP-адресов
```
ip.addr == 192.168.1.100 || ip.addr == 10.0.0.5
```
**Применение:** Анализ трафика между двумя конкретными хостами

---

#### 5. Исключение трафика
```
!(ip.addr == 192.168.1.1)
```
**Применение:** Скрыть трафик шлюза/маршрутизатора для фокуса на хостах

---

#### 6. Подсеть
```
ip.src == 192.168.1.0/24
```
**Применение:** Показать весь трафик из локальной сети

---

### Фильтры по протоколам

#### 7. HTTP трафик
```
http
```
**Применение:** Показать все HTTP пакеты (запросы и ответы)

---

#### 8. DNS трафик
```
dns
```
**Применение:** Показать все DNS запросы и ответы

---

#### 9. Комбинация IP и протокола
```
ip.src == 192.168.1.100 && http
```
**Применение:** HTTP-запросы от конкретного зараженного хоста

---

#### 10. Несколько протоколов
```
http || dns || nbns
```
**Применение:** Показать HTTP, DNS и NetBIOS трафик одновременно

---

#### 11. Трафик на TCP порт
```
tcp.port == 443
```
**Применение:** Показать HTTPS трафик (порт 443)

---

#### 12. Трафик на UDP порт
```
udp.port == 53
```
**Применение:** DNS трафик (работает на UDP порту 53)

---

### Фильтры по содержимому

#### 13. Поиск строки в пакете
```
frame contains "malware"
```
**Применение:** Найти пакеты, содержащие конкретную строку

---

#### 14. Поиск строки в HTTP
```
http contains "eval("
```
**Применение:** Обнаружение обфусцированного JavaScript кода

---

#### 15. Фильтр по размеру пакета
```
frame.len > 1000
```
**Применение:** Большие пакеты могут содержать загрузку вредоносных файлов

---

---

## Статистика и анализ

### 1. Endpoints (Конечные точки)

**Путь:** `Statistics → Endpoints`

**Что показывает:**
- Все IP/MAC адреса в захвате
- Количество отправленных/полученных пакетов
- Количество переданных байт

**Применение:**
1. Перейдите на вкладку **IPv4**
2. Кликните на заголовок **Packets** для сортировки
3. **Самый активный IP** — вероятно зараженный хост

**Пример:**
```
Address          | Packets | Bytes   | Tx Packets | Rx Packets
──────────────────────────────────────────────────────────────
192.168.1.100    | 15423   | 8.2 MB  | 7800       | 7623      ← Подозрительный
192.168.1.1      | 2341    | 1.1 MB  | 1200       | 1141
10.5.7.89        | 4123    | 512 KB  | 2100       | 2023      ← Внешний сервер?
```

**💡 Совет:** Правый клик на IP → **Apply as Filter → Selected** для быстрого фильтра

---

### 2. Conversations (Соединения)

**Путь:** `Statistics → Conversations`

**Что показывает:**
- Все TCP/UDP соединения между двумя хостами
- Количество пакетов и байт в каждом соединении

**Применение:**
1. Откройте вкладку **TCP**
2. Сортируйте по **Bytes** (количество переданных данных)
3. Ищите соединения с большим объемом данных к внешним IP

**Пример:**
```
Address A        | Port A | Address B    | Port B | Packets | Bytes
────────────────────────────────────────────────────────────────────
192.168.1.100    | 49234  | 10.5.7.89    | 80     | 234     | 1.2 MB  ← C2?
192.168.1.100    | 49235  | 93.184.216.34| 443    | 54      | 23 KB
```

**💡 Совет:** Правый клик → **Follow → TCP Stream** для просмотра всего соединения

---

### 3. Protocol Hierarchy (Иерархия протоколов)

**Путь:** `Statistics → Protocol Hierarchy`

**Что показывает:**
- Процентное соотношение всех протоколов в захвате
- Дерево протоколов (Ethernet → IP → TCP → HTTP)

**Применение:**
Идентификация необычных протоколов или аномально большого трафика определенного типа

**Пример вывода:**
```
Protocol                    | % Packets | Packets | Bytes
────────────────────────────────────────────────────────────
Frame                       | 100%      | 15423   | 8.2 MB
  Ethernet                  | 100%      | 15423   | 8.2 MB
    IP                      | 98.5%     | 15192   | 8.1 MB
      TCP                   | 85.2%     | 13145   | 7.8 MB
        HTTP                | 45.3%     | 6987    | 4.2 MB  ← Большая часть HTTP
        SSL/TLS             | 20.1%     | 3100    | 2.1 MB
      UDP                   | 13.3%     | 2047    | 300 KB
        DNS                 | 12.8%     | 1974    | 280 KB
```

**Признаки атаки:**
- Аномально высокий процент HTTP (может быть C2-коммуникация)
- Необычные протоколы (IRC, нестандартные порты)

---

### 4. HTTP Statistics

**Путь:** `Statistics → HTTP → Requests`

**Что показывает:**
- Список всех HTTP-хостов с количеством запросов
- Топ запрашиваемых доменов

**Применение:**
Быстрое определение вредоносных доменов с большим количеством запросов

**💡 Совет:** Экспортируйте список и проверьте домены через VirusTotal

---

---

## Анализ HTTP трафика

### Фильтры для HTTP

#### 1. Только HTTP-запросы
```
http.request
```
**Применение:** Показать все HTTP-запросы (GET, POST и т.д.)

---

#### 2. Только HTTP-ответы
```
http.response
```
**Применение:** Показать все ответы от серверов

---

#### 3. Запросы к конкретному домену
```
http.host == "evil.com"
```
**Применение:** Проверка трафика к подозрительному домену

---

#### 4. Домен содержит подстроку
```
http.host contains "malicious"
```
**Применение:** Поиск всех доменов, содержащих ключевое слово

---

#### 5. GET-запросы
```
http.request.method == "GET"
```
**Применение:** Анализ загрузок файлов

---

#### 6. POST-запросы
```
http.request.method == "POST"
```
**Применение:** Обнаружение отправки данных к C2-серверу (exfiltration)

**💡 POST-запросы важны:** Обычно указывают на передачу данных от жертвы к атакующему

---

#### 7. Фильтр по URI (путь в URL)
```
http.request.uri contains "/malware/"
```
**Применение:** Поиск запросов с подозрительными путями

---

#### 8. HTTP Referer
```
http.referer contains "excelforum.com"
```
**Применение:** Определение источника заражения (с какого сайта пришел пользователь)

---

#### 9. User-Agent
```
http.user_agent contains "MSIE"
```
**Применение:** Определение браузера/ОС жертвы

**Подозрительные User-Agent:**
```
http.user_agent contains "curl"
http.user_agent contains "python"
http.user_agent contains "powershell"
```
Эти строки указывают на автоматизированные запросы (скрипты, вредоносы)

---

#### 10. HTTP коды ответа

**Успешные запросы (200 OK):**
```
http.response.code == 200
```

**Редиректы (301/302):**
```
http.response.code == 302 || http.response.code == 301
```
**Применение:** Анализ цепочки перенаправлений в exploit kit атаках

**Ошибки (404 Not Found):**
```
http.response.code == 404
```

---

#### 11. Content-Type

**JavaScript:**
```
http.content_type contains "javascript"
```
**Применение:** Обнаружение вредоносных JS-скриптов

**Исполняемые файлы:**
```
http.content_type contains "application/x-msdownload"
```
**Применение:** Обнаружение загрузки .exe файлов

**ZIP/RAR архивы:**
```
http.content_type contains "application/zip"
```

---

### Практический пример: Анализ цепочки заражения

**Шаг 1:** Найдите начальный запрос
```
ip.src == 192.168.1.100 && http.request
```

**Шаг 2:** Правый клик на первый HTTP-пакет → **Follow → HTTP Stream**

**Шаг 3:** Анализируйте Referer и редиректы
```
http.referer || (http.response.code == 302)
```

**Шаг 4:** Ищите загрузку вредоносных файлов
```
http.request.uri contains ".exe" || http.request.uri contains ".zip"
```

---

---

## Анализ DNS трафика

### Фильтры для DNS

#### 1. Только DNS-запросы
```
dns.flags.response == 0
```
**Применение:** Показать только запросы (исключить ответы)

---

#### 2. Только DNS-ответы
```
dns.flags.response == 1
```
**Применение:** Показать только ответы с IP-адресами

---

#### 3. Запрос к конкретному домену
```
dns.qry.name == "malicious.com"
```
**Применение:** Проверка обращения к известному вредоносному домену

---

#### 4. Домен содержит подстроку
```
dns.qry.name contains "evil"
```
**Применение:** Поиск доменов с подозрительными ключевыми словами

---

#### 5. Запросы от конкретного хоста
```
ip.src == 192.168.1.100 && dns
```
**Применение:** DNS-активность зараженного хоста

---

#### 6. DNS запросы с длинными именами
```
dns && frame.len > 100
```
**Применение:** Обнаружение DNS-туннелинга (exfiltration через DNS)

**💡 Нормальные DNS-запросы обычно < 80 байт**

---

### Экспорт DNS-запросов

**Шаг 1:** Примените фильтр
```
dns.flags.response == 0
```

**Шаг 2:** `File → Export Packet Dissections → As Plain Text`

**Шаг 3:** В опциях экспорта:
- ✅ All packets (Displayed)
- ✅ Packet summary line
- Сохраните как `dns_queries.txt`

**Шаг 4:** Проверьте домены через VirusTotal или Shodan

---

### Практический пример: Поиск C2 домена

**Шаг 1:** Найдите DNS-запросы от зараженного хоста
```
ip.src == 192.168.1.100 && dns.flags.response == 0
```

**Шаг 2:** Посмотрите список доменов в колонке **Info**

**Шаг 3:** Ищите подозрительные паттерны:
- Случайные строки: `a83js92jd.com`
- DGA (Domain Generation Algorithm): `xjk239djk.net`
- Необычные TLD: `.tk`, `.cc`, `.biz`

**Шаг 4:** Найдите соответствующий HTTP-трафик
```
http.host == "найденный_домен.com"
```

---

---

## Реконструкция TCP потоков

### Зачем нужны TCP Streams?

TCP-поток (stream) объединяет все пакеты одного TCP-соединения в единый диалог.
Это позволяет увидеть полное содержимое HTTP-запроса/ответа, передачу файлов, и т.д.

### Как открыть TCP Stream

**Способ 1: Правый клик на пакете**
1. Найдите интересующий TCP-пакет
2. Правый клик → **Follow → TCP Stream**

**Способ 2: Через меню**
1. Выберите TCP-пакет
2. **Analyze → Follow → TCP Stream**

### Интерфейс TCP Stream

После открытия вы увидите окно с содержимым соединения:

```
┌─────────────────────────────────────────────┐
│ [ Stream 0 ▼]  [Find...]  [▲] [▼]  [Close] │
├─────────────────────────────────────────────┤
│ GET /malware.exe HTTP/1.1               🔴 │ ← Запрос (красный)
│ Host: evil.com                              │
│ User-Agent: Mozilla/5.0                     │
│                                             │
│ HTTP/1.1 200 OK                         🔵 │ ← Ответ (синий)
│ Content-Type: application/octet-stream      │
│ Content-Length: 245760                      │
│                                             │
│ MZ.....[бинарные данные]....                │
└─────────────────────────────────────────────┘
```

**Цвета:**
- 🔴 **Красный** — данные, отправленные клиентом (запрос)
- 🔵 **Синий** — данные, отправленные сервером (ответ)

### Навигация между потоками

В верхней части окна есть **[ Stream X ]** dropdown:
- Используйте стрелки **▲ ▼** для перехода между потоками
- Или введите номер потока вручную

### Форматы отображения

В выпадающем меню внизу окна можно выбрать формат:

| Формат | Описание |
|--------|----------|
| **ASCII** | Текстовое представление (по умолчанию) |
| **EBCDIC** | Редко используемая кодировка |
| **Hex Dump** | Шестнадцатеричный и ASCII дамп |
| **C Arrays** | Формат C-массива для программирования |
| **Raw** | Сырые байты без форматирования |

**💡 Совет:** Для анализа бинарных файлов используйте **Hex Dump**

### Сохранение потока

**Шаг 1:** Откройте TCP Stream

**Шаг 2:** Нажмите **Save as...**

**Шаг 3:** Выберите направление:
- **Entire conversation** — весь диалог (запрос + ответ)
- **Client → Server** — только запрос
- **Server → Client** — только ответ (часто нужно для файлов)

**Применение:**
Сохраните вредоносный файл, переданный по HTTP, для последующего анализа

---

### Практический пример: Извлечение файла

**Сценарий:** Зараженный хост загрузил `malware.exe` по HTTP

**Шаг 1:** Найдите HTTP-запрос на файл
```
http.request.uri contains ".exe"
```

**Шаг 2:** Правый клик на найденном пакете → **Follow → HTTP Stream**

**Шаг 3:** В окне потока вы увидите:
```
GET /downloads/malware.exe HTTP/1.1
Host: evil-server.com

HTTP/1.1 200 OK
Content-Type: application/octet-stream

MZ....PE...[бинарные данные].....
```

**Шаг 4:** Нажмите **Save as...**

**Шаг 5:** Выберите **Server → Client** (чтобы сохранить только ответ сервера)

**Шаг 6:** Сохраните как `extracted_malware.exe`

**⚠️ ВНИМАНИЕ:** Не запускайте извлеченный файл! Анализируйте в изолированной среде.

---

---

## Экспорт объектов

### HTTP Objects Export

**Путь:** `File → Export Objects → HTTP...`

Это открывает список всех HTTP-объектов (файлов), переданных в захвате.

### Интерфейс окна

```
┌─────────────────────────────────────────────────────────────────┐
│ Packet | Hostname        | Content Type        | Size    | File │
├─────────────────────────────────────────────────────────────────┤
│ 234    | evil.com        | text/html           | 4523    | index.html
│ 456    | evil.com        | application/x-msdownload | 245760 | malware.exe ←
│ 789    | cdn.jquery.com  | application/javascript | 86234  | jquery.min.js
└─────────────────────────────────────────────────────────────────┘
[Save All] [Save] [Close]
```

### Как использовать

**Шаг 1:** Откройте `File → Export Objects → HTTP`

**Шаг 2:** Просмотрите список файлов

**Шаг 3:** Обратите внимание на:
- **Content Type:** `application/x-msdownload`, `application/octet-stream` (исполняемые файлы)
- **Size:** Большие файлы могут быть вредоносными
- **Hostname:** Подозрительные домены

**Шаг 4:** Выберите интересующий файл

**Шаг 5:** Нажмите **Save** или **Save All**

### Фильтрация объектов

**Поиск исполняемых файлов:**
- В колонке **Content Type** ищите `application/x-msdownload` или `application/octet-stream`
- В колонке **Filename** ищите `.exe`, `.dll`, `.scr`

**Поиск архивов:**
- Content Type: `application/zip`, `application/x-rar`

**Поиск скриптов:**
- Content Type: `application/javascript`, `text/javascript`

### Практический пример

**Сценарий:** Экспорт всех загруженных файлов для анализа

**Шаг 1:** `File → Export Objects → HTTP`

**Шаг 2:** Кликните **Save All**

**Шаг 3:** Выберите папку для сохранения (например, `exported_objects/`)

**Шаг 4:** Все файлы будут сохранены с оригинальными именами

**Шаг 5:** Проанализируйте файлы:
```bash
cd exported_objects/
file *                    # Определить тип файлов
sha256sum *              # Вычислить хеши
ls -lh                   # Посмотреть размеры
```

**Шаг 6:** Проверьте хеши на VirusTotal

---

---

## Продвинутые техники

### 1. Фильтры на основе времени

#### Пакеты в определенном временном окне
```
frame.time >= "2024-01-15 14:30:00" && frame.time <= "2024-01-15 14:35:00"
```
**Применение:** Фокусировка на периоде инцидента

---

#### Временной интервал (относительный)
```
frame.time_relative < 60
```
**Применение:** Первые 60 секунд захвата (начало атаки)

---

### 2. Фильтры по флагам TCP

#### SYN пакеты (начало соединения)
```
tcp.flags.syn == 1 && tcp.flags.ack == 0
```
**Применение:** Обнаружение сканирования портов или большого количества новых соединений

---

#### RST пакеты (сброс соединения)
```
tcp.flags.reset == 1
```
**Применение:** Обнаружение отклоненных соединений

---

#### Retransmissions (повторные передачи)
```
tcp.analysis.retransmission
```
**Применение:** Проблемы с сетью или DoS-атака

---

### 3. Комбинированные сложные фильтры

#### POST-запросы от хоста к внешним IP
```
ip.src == 192.168.1.100 && http.request.method == "POST" && !(ip.dst == 192.168.0.0/16)
```
**Применение:** Обнаружение exfiltration данных

---

#### DNS и последующий HTTP к тому же домену
```
(dns.qry.name == "evil.com") || (http.host == "evil.com")
```
**Применение:** Полная цепочка: DNS-запрос → HTTP-коммуникация

---

#### Исключение легитимного трафика
```
!(http.host contains "google" || http.host contains "microsoft" || http.host contains "cloudflare")
```
**Применение:** Фильтрация только подозрительных доменов

---

### 4. Поиск строк в трафике

#### Поиск команд PowerShell
```
http contains "powershell"
```
**Применение:** Обнаружение скриптовых атак

---

#### Поиск команд cmd.exe
```
frame contains "cmd.exe"
```
**Применение:** Обнаружение выполнения команд

---

#### Поиск SQL инъекций
```
http.request.uri contains "SELECT" || http.request.uri contains "UNION"
```
**Применение:** Обнаружение попыток SQL-инъекций

---

### 5. Работа с закладками (Bookmarks)

Wireshark позволяет сохранять важные пакеты как закладки.

**Как создать закладку:**
1. Выберите пакет
2. **Edit → Set Bookmark** (или `Ctrl+B`)

**Переход к закладкам:**
- **Edit → Next Bookmark** (`Shift+Ctrl+N`)
- **Edit → Previous Bookmark** (`Shift+Ctrl+B`)

**Применение:**
Пометить ключевые моменты атаки (начальный запрос, загрузка файла, POST к C2)

---

### 6. Time Display Format (Формат времени)

По умолчанию Wireshark показывает время относительно начала захвата.

**Изменение формата:**
1. **View → Time Display Format**
2. Выберите:
   - **Date and Time of Day** — абсолютное время (2024-01-15 14:32:45)
   - **Seconds Since Beginning of Capture** — секунды от начала (по умолчанию)
   - **Seconds Since Previous Displayed Packet** — дельта между пакетами

**Применение:**
Для отчетов используйте абсолютное время

---

### 7. Expert Information (Экспертная информация)

**Путь:** `Analyze → Expert Information` (или иконка в левом нижнем углу)

Wireshark автоматически обнаруживает проблемы:

| Уровень | Описание |
|---------|----------|
| 🔴 **Errors** | Критические ошибки (malformed packets) |
| ⚠️ **Warnings** | Предупреждения (retransmissions, fast retransmissions) |
| 💬 **Notes** | Примечания (duplicate ACKs) |
| 💡 **Chats** | Информационные сообщения (HTTP requests) |

**Применение:**
Быстрая диагностика проблем в захвате

---

---

## Практические примеры анализа атак

### Пример 1: Анализ Emotet malware infection

**Сценарий:**
Пользователь открыл вредоносное вложение в email, произошло заражение Emotet.

**Цель:**
Найти зараженный хост, C2-серверы, и цепочку заражения.

---

**ШАГ 1: Определение зараженного хоста**

```
Statistics → Endpoints → IPv4 → Сортировка по Packets
```

Результат:
```
Address          | Packets
──────────────────────────
192.168.1.105    | 8432    ← Самый активный
```

---

**ШАГ 2: Поиск первоначального контакта (HTTP)**

Фильтр:
```
ip.src == 192.168.1.105 && http.request
```

Результат:
```
Packet | Time   | Source         | Destination   | Info
─────────────────────────────────────────────────────────────
142    | 0.823  | 192.168.1.105 | 10.4.5.89     | GET /invoice.doc HTTP/1.1
```

**Правый клик → Follow → HTTP Stream**

Видим:
```
GET /invoice.doc HTTP/1.1
Host: malicious-domain.com
Referer: https://excelforum.com/some-post    ← Источник заражения!
```

---

**ШАГ 3: Поиск загрузки payload**

Фильтр:
```
ip.src == 192.168.1.105 && (http.request.uri contains ".exe" || http.request.uri contains ".dll")
```

Результат:
```
Packet | Info
──────────────────────────────────────────
456    | GET /stage2.exe HTTP/1.1
```

**Follow → HTTP Stream → Save as... → Server → Client**
Сохранить как `emotet_payload.exe`

---

**ШАГ 4: Поиск C2-коммуникации (POST запросы)**

Фильтр:
```
ip.src == 192.168.1.105 && http.request.method == "POST"
```

Результат:
```
Packet | Destination   | Info
────────────────────────────────────────
789    | 45.34.12.67  | POST /gate.php HTTP/1.1
812    | 91.203.45.12 | POST /check.php HTTP/1.1
```

Это **C2-серверы Emotet**.

---

**ШАГ 5: Экспорт IoC (Indicators of Compromise)**

**DNS домены:**
```
Statistics → Resolved Addresses → Export
```

**IP адреса C2:**
```
45.34.12.67
91.203.45.12
```

**Домены:**
```
malicious-domain.com
```

**Хеш файла:**
```bash
sha256sum emotet_payload.exe
```

---

**ИТОГ:**
- Зараженный хост: `192.168.1.105`
- Источник заражения: `excelforum.com`
- Вредоносный домен: `malicious-domain.com`
- C2 серверы: `45.34.12.67`, `91.203.45.12`
- Payload: `emotet_payload.exe` (SHA256: ...)

---

### Пример 2: Обнаружение DNS Tunneling (Exfiltration)

**Сценарий:**
Атакующий использует DNS для exfiltration конфиденциальных данных.

**Признаки:**
- Необычно длинные DNS-запросы
- Большое количество DNS-запросов к одному домену

---

**ШАГ 1: Поиск длинных DNS-запросов**

Фильтр:
```
dns && frame.len > 100
```

Результат:
```
Packet | Info
────────────────────────────────────────────────────────────────────
123    | Standard query A a83js92jd83hd73kd83js92.evil-dns.com
234    | Standard query A b94kd83js92jd83hd73kd83js92.evil-dns.com
```

**💡 Обычные DNS-запросы < 80 байт!**

---

**ШАГ 2: Подсчет частоты запросов**

```
Statistics → DNS → Export
```

Или в командной строке:
```bash
tshark -r capture.pcap -Y "dns.qry.name" -T fields -e dns.qry.name | \
grep "evil-dns.com" | wc -l
```

Результат: **452 запроса** к `evil-dns.com` — это аномально!

---

**ШАГ 3: Анализ поддоменов**

Фильтр:
```
dns.qry.name contains "evil-dns.com"
```

Видим паттерн:
```
a83js92jd83hd73kd.evil-dns.com
b94kd83js92jd83h.evil-dns.com
c73kd83js92jd83h.evil-dns.com
```

Случайные строки = **закодированные данные**!

---

**ШАГ 4: Декодирование (пример)**

Поддомены могут быть base64/hex кодированными данными.

Экспортируйте все запросы:
```bash
tshark -r capture.pcap -Y "dns.qry.name contains 'evil-dns.com'" \
-T fields -e dns.qry.name | sed 's/.evil-dns.com//' > encoded_data.txt
```

Попробуйте декодировать:
```bash
cat encoded_data.txt | base64 -d
```

---

**ИТОГ:**
- Обнаружен DNS Tunneling к домену `evil-dns.com`
- 452 аномальных DNS-запроса
- Данные закодированы в поддоменах

**Рекомендации:**
- Блокировать домен `evil-dns.com`
- Изолировать зараженный хост
- Проверить какие данные были украдены

---

### Пример 3: Exploit Kit (Drive-by Download)

**Сценарий:**
Пользователь посетил взломанный сайт, который перенаправил его на exploit kit.

**Цель:**
Найти цепочку редиректов и вредоносный payload.

---

**ШАГ 1: Найти начальный сайт**

Фильтр:
```
ip.src == 192.168.1.100 && http.request
```

Сортируйте по времени и найдите первый HTTP-запрос:
```
Packet | Info
──────────────────────────────────────────
10     | GET / HTTP/1.1
        Host: legitimate-site.com
```

---

**ШАГ 2: Найти редиректы (302)**

Фильтр:
```
ip.dst == 192.168.1.100 && (http.response.code == 302 || http.response.code == 301)
```

Результат:
```
Packet | Info
──────────────────────────────────────────────────────────────
15     | HTTP/1.1 302 Found
        Location: http://malicious-redirect.net/landing
```

**Follow → HTTP Stream**:
```
HTTP/1.1 302 Found
Location: http://exploit-kit.com/exploit?id=abc123
```

---

**ШАГ 3: Найти вредоносный JavaScript**

Фильтр:
```
http.content_type contains "javascript"
```

**Follow → HTTP Stream**

Видим обфусцированный JavaScript:
```javascript
eval(unescape("%75%61%6c%28%66%75%6e%63%74%69%6f%6e..."));
```

---

**ШАГ 4: Найти payload**

Фильтр:
```
http.request.uri contains ".exe" || http.content_type contains "application/x-msdownload"
```

Результат:
```
Packet | Info
──────────────────────────────────────────────
234    | GET /payload.exe HTTP/1.1
```

**File → Export Objects → HTTP → Сохранить `payload.exe`**

---

**ШАГ 5: Построение цепочки**

```
legitimate-site.com (взломан)
    ↓ 302 redirect
malicious-redirect.net/landing
    ↓ JavaScript redirect
exploit-kit.com/exploit
    ↓ Эксплуатация уязвимости браузера
    ↓ Загрузка
payload.exe (Malware)
```

---

**ИТОГ:**
- Начальный сайт: `legitimate-site.com` (скомпрометирован)
- Exploit kit: `exploit-kit.com`
- Payload: `payload.exe` (SHA256: ...)

**Рекомендации:**
- Уведомить владельца `legitimate-site.com` о компрометации
- Блокировать `exploit-kit.com`
- Проверить `payload.exe` на вредоносность

---

---

## Полезные горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl+F` | Поиск пакета (Find Packet) |
| `Ctrl+N` | Следующий найденный пакет |
| `Ctrl+B` | Установить закладку (Bookmark) |
| `Ctrl+G` | Перейти к пакету по номеру (Go to Packet) |
| `Ctrl+↑` / `Ctrl+↓` | Следующий/предыдущий пакет в списке |
| `Ctrl+→` / `Ctrl+←` | Переход по истории (History) |
| `Ctrl+E` | Открыть Expert Information |
| `Ctrl+Shift+E` | Expression builder (построитель фильтров) |

---

## Советы и Best Practices

### ✅ DO (Делайте)

1. **Начинайте с Endpoints и Conversations**
   Это быстрый способ найти самого активного хоста (зараженного)

2. **Используйте Follow TCP Stream**
   Для просмотра полного диалога между клиентом и сервером

3. **Экспортируйте HTTP Objects**
   Чтобы извлечь все загруженные файлы для анализа

4. **Проверяйте Referer и редиректы**
   Для понимания цепочки заражения (infection chain)

5. **Сохраняйте все IoC (домены, IP, хеши)**
   Для последующей блокировки и отчетности

6. **Используйте цветовую кодировку**
   Настройте свои цвета для быстрого визуального анализа

7. **Делайте закладки (Bookmarks)**
   На ключевых пакетах (начало атаки, C2 коммуникация, etc.)

---

### ❌ DON'T (Не делайте)

1. **Не пытайтесь анализировать весь захват сразу**
   Используйте фильтры для фокусировки на конкретном хосте

2. **Не игнорируйте Expert Information**
   Wireshark автоматически обнаруживает аномалии

3. **Не забывайте про DNS**
   DNS-запросы часто предшествуют HTTP-коммуникации

4. **Не запускайте извлеченные файлы!**
   Анализируйте только в изолированной среде (виртуальная машина)

5. **Не полагайтесь только на Wireshark**
   Используйте дополнительные инструменты (VirusTotal, Shodan, AbuseIPDB)

---

## Полезные ресурсы

### Документация
- **Официальный User Guide:** https://www.wireshark.org/docs/wsug_html_chunked/
- **Display Filters Reference:** https://www.wireshark.org/docs/dfref/
- **Wiki:** https://wiki.wireshark.org/

### Тренировочные PCAP файлы
- **Malware-Traffic-Analysis.net:** https://malware-traffic-analysis.net/
  - Реальные PCAP файлы заражений вредоносами
  - С подробными разборами (write-ups)
- **Wireshark Sample Captures:** https://wiki.wireshark.org/SampleCaptures

### Проверка IoC
- **VirusTotal:** https://www.virustotal.com/ (хеши файлов, IP, домены)
- **AbuseIPDB:** https://www.abuseipdb.com/ (проверка IP)
- **Shodan:** https://www.shodan.io/ (информация о серверах)
- **URLhaus:** https://urlhaus.abuse.ch/ (вредоносные URL)

---

## Заключение

Wireshark — мощный инструмент для анализа сетевого трафика и обнаружения атак.

**Основные этапы анализа:**
1. Открыть PCAP → Endpoints → найти самого активного хоста
2. Применить фильтр на этот хост → анализировать HTTP и DNS
3. Follow TCP Stream для детального просмотра коммуникаций
4. Экспортировать HTTP Objects для извлечения файлов
5. Собрать IoC (IP, домены, хеши) для блокировки

**Практика — ключ к мастерству!**
Скачайте тренировочные PCAP с https://malware-traffic-analysis.net/ и практикуйтесь.

---

**Дата создания:** 2025-12-05
**Версия:** 1.0
